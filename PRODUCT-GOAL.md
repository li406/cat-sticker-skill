# PRODUCT-GOAL

## 一句话目标

这个产品帮助 **Owner（一个养猫、想做微信表情包的普通用户）** 完成 **把自己猫咪的照片变成成套微信静态表情包**，让用户最终能够 **直接拿到可上传微信表情开放平台的一整套素材（主图、封面、图标、横幅、ZIP），而不需要自己用 PS 或第三方 App 逐张修图**。

## 为什么做

Owner 养了猫，平时想用自己家猫做微信聊天表情，但市面上的表情包工具要么是模板化的"AI 换脸"不贴自家猫，要么需要手动抠图、加字、调尺寸，做一套九张要折腾很久。这个工具让用户把猫照片丢给 AI 助手，助手自动看懂猫长什么样、规划表情文案、在花钱前给用户看方案、确认后才调用付费生图 API，最后自动完成抠透明底、加字、按微信规格打包。

核心价值：**花钱前先批准、失败不重复扣费、产物直接符合微信上传规格、私人猫图不出本机。**

## V1 必须做到

- [ ] 用户给一张或多张自家猫照片，AI 助手能理解猫的外貌特征并记录
- [ ] AI 自动规划一套表情（每张有文案、情绪、姿势），**付费前展示完整计划供用户批准/修改**
- [ ] 批准后调用 Seedream 生成无文字干净底图
- [ ] 本地完成：抠透明背景 + 叠加文案 + 缩到 240×240
- [ ] 单张失败可重试、单张不满意可重生成、版本历史保留、断点可恢复
- [ ] 一键导出微信静态素材包（透明主图、白底封面、50×50 图标、750×400 横幅、ZIP）
- [ ] API Key 只走环境变量，私人猫图和成品不进 git 仓库

## V1 成功标准

作为用户（Owner），我能够：

1. 用自己真实的猫照片，通过 AI 助手自然语言交互，做出 **一套完整、能上传微信表情开放平台** 的静态表情包
2. 全程知道每一次付费生图花了几次钱；没批准的计划绝不扣费
3. 做出的表情在微信聊天里发出去是透明底、文案清晰、不挡猫脸，成套风格统一

## 明确不做

- GIF / 动图 / 视频表情
- Web UI、Gradio、FastAPI、React/Vue、桌面 GUI、SaaS
- 微信自动登录、自动投稿、自动过审
- 第二个视觉大模型 API（视觉理解完全由宿主 Agent 自己做）
- 账户系统、云同步
- 把工具做成需要用户手动敲命令行的独立 CLI 产品（交互界面是 AI Agent）

## 不可突破边界

- **API Key 绝不落盘、绝不进日志、绝不进 fixture**，只从 `ARK_API_KEY` 环境变量读取
- **私人猫照片和成品绝不进 git 仓库**，只存本地 workspace；公开测试只用合成 fixture
- **付费门不可绕过**：未批准计划不发生真实 Seedream 请求；真实付费调用次数有界、可中断、不无限重试
- **隐私扫描是发布硬门**：tracked 文件不得含 secret、私人绝对路径、未授权图片
- 许可证 PolyForm Noncommercial 1.0.0，未经 Owner 许可不得商用
