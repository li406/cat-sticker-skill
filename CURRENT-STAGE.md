# CURRENT-STAGE

## Stage

Stage 2 — 换猫换风格再做一套，默认质量稳定

## 唯一目标

完成后，用户能够：

> 换一只猫、换一种文案风格再做一套表情，第二次做比第一次更省心：透明底边缘干净、文案不挡猫脸、风格不再只有一种黄字黑边，抠图和排版策略以后默认生效。

## 必须完成

- [ ] 用第二只猫（或同一只猫另一批风格差异大的照片）完整做一套，不出现 Stage 1 没有的新问题
- [ ] 透明底主图在微信聊天里边缘干净，肉眼看不到一圈白边
- [ ] 至少能切换一种和默认黄字黑边明显不同的文案风格，成品仍可读、不挡猫脸
- [ ] 较长文案（约 10 字以上 / 两句）自动换行、不溢出画布、不压猫脸
- [ ] 选定的抠图策略变成默认值，下次做新表情不再需要手动调

## Allowed Scope

- 用真实猫图做第二套验收，必要时根据反馈微调排版 preset 选择和抠图策略
- 为达成验收所必需的最小代码调整（如选定默认 matting_strategy、长文案换行逻辑）
- 过程中发现的真正 Blocker 当场修

## 明确不做

- 不开源文档、README 打磨、其他 Agent 兼容矩阵（Stage 3）
- 不做 Codex 双端验收
- 不做 GIF / 动图 / 第二 provider / Web UI
- 不一次性铺开所有排版 preset 的全量回归
- 不为代码优雅做重构
- 不重新设计 banner（Stage 1 已接受现状；若成为新痛点由 Owner 单独提）

## Owner 验收步骤

1. 跟 AI 助手说"换第二只猫，做一套"，换一批照片
2. 看这次默认产出的透明底图，边缘干净
3. 让 AI 换一种字风做其中一张，对比效果
4. 给一句长梗（例如"今天也是不想上班的一天呢"），看自动换行排版正常
5. 把成品发微信聊天，肉眼确认边缘和文案效果
6. 说"以后就用这个抠图风格"，下次新表情不再需要调

期望：

- 全程 AI 助手主动报告进展，不需要 Owner 猜下一步
- 第二次做比第一次省心，不反复手动说"抠干净点""字换个颜色"
- 没有未批准的付费调用、没有 key 泄露

## Blocker

只有以下问题可以阻止阶段完成：

- 换一只猫就跑不通（报错、生成失败、图根本没法看）
- 两种抠图策略在真实猫图上都有严重白边/黑底，微信里不能用
- 新排版风格做出来是坏的：中文乱码、字看不清、大面积挡猫脸
- 长文案必然溢出或必然压脸

其他小毛病（某张文案不够好笑、某个字位置偏一点）都是 Polish，进 Backlog。

## Stop Condition

当：

- 所有 Owner 验收步骤完成
- 没有 Blocker

则 Stage 2 立即结束。过程中发现的 Important / Polish 进入 BACKLOG.md，不继续在本阶段打磨。
