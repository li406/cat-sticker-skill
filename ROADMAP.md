# ROADMAP

当前阶段：Stage 1

## 已完成

### Stage 0 — 工具链骨架与 Mock 全链路

用户获得：

> 一套在工程上可重复验证的工具链：建项目、加参考图、导入角色、规划、批准、mock 生成、本地加字抠图、微信包导出、隐私扫描，全程不花一分钱 API 费也能跑通。CI 在 Ubuntu + Windows 矩阵全绿。

状态：done（commit `2e75577`，66 测试通过，smoke-test PASS，privacy-check PASS）

说明：本阶段所有"生成图片"都用 mock 合成图代替，证明的是管道和状态机正确，不是出图质量。

## 当前

### Stage 1 — Owner 真实猫图跑通一套并上传微信成功

用户获得：

> Owner 第一次用自己真实的猫照片 + 真实付费 Seedream，在 AI 助手陪伴下做出 **一套完整、可直接上传微信表情开放平台、发出去好看** 的表情包，并完成一次真实上传。

状态：active

完成条件：

- [ ] Owner 用真实猫照片完整走一遍：加参考图 → Agent 规划 → 批准 → 生成 → 调整 → 打包
- [ ] 产出的 ZIP 包含微信要求的全部素材，尺寸/底色正确
- [ ] 至少在微信表情开放平台完成一次上传提交（过审与否不阻塞，提交动作本身完成）
- [ ] 透明底主图发在微信聊天里没有难看的白边/黑底
- [ ] Banner 图是单独设计感的横图，不是主图强行拉伸

## 后续

### Stage 2 — 质量打磨与私人回归

用户获得：

> 同一套工具换不同猫、不同文案风格也能稳定出好图；透明底白边问题用真实猫图回归选定默认抠图策略；排版风格不再只有一种黄字黑描边。

状态：planned

### Stage 3 — 开源发布与其他 Agent 兼容

用户获得：

> 其他 GitHub 用户/其他 AI Agent 拿到这个仓库也能照着 SKILL.md 用；README 写清真实实测过的环境，不夸大兼容性。

状态：planned

## 路线规则

- 同时只有一个 active Stage
- 非当前问题进入 BACKLOG
- 不设没有用户价值的纯清理 Stage，除非它是必要前置
- Roadmap 里的"用户获得"比技术细节重要；描述的是 Owner 实际多能做什么，不是改了多少代码
